[build]
  base = "client"
  command = "npm install && npm run build"
  publish = "dist"

# VITE_API_URL wird pro Site im Netlify-Dashboard bzw. per CLI gesetzt
# (Test-Site und später teigmaster zeigen auf rezeptrechner-api.onrender.com).

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
